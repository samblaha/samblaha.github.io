# Copy to scripts/.env (gitignored) and `set -a; source scripts/.env; set +a`
# or export these in your shell. Never commit real values. Never put them in HTML/JS.
GHIN_EMAIL=
GHIN_PASSWORD=
# Optional: only needed if login does not return a golfer id
# GHIN_ID=
